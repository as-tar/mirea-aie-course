# Самопроверка проекта (self-checklist)

Отметьте, что реализовано в вашем проекте.  
Этот чеклист помогает и вам, и преподавателю быстро понять, на каком уровне готовности находится проект.

В столбце «Да/Нет (студент)» поставьте `✅` или `❌`.  
В столбце «Где смотреть / комментарий» укажите файл(ы), где реализован соответствующий пункт.

---

## Таблица самопроверки

| #  | Критерий                                                                 | Да/Нет (студент) | Где смотреть / комментарий                          |
|----|---------------------------------------------------------------------------|------------------|-----------------------------------------------------|
| 1  | Сервис запускается по инструкциям из `project/README.md` и работает      |        Да        | `README.md`, раздел «Как запустить проект»       |
| 2  | Endpoint `/predict` (прим. в данном случае `/ask`) использует **реальную модель**, а не заглушку        |        Да        | `src/retriever/`, `src/pipelines/`, `src/service/`   |
| 3  | Есть EDA и хотя бы один эксперимент с метриками                          |        Да        | `notebooks/01_eda.ipynb`, `notebooks/02_experiments_and_evaluation.ipynb`, `report.md`     |
| 4  | Есть baseline и улучшенная модель, есть **сравнение по метрикам**        |        Да        | `notebooks/02_experiments_and_evaluation.ipynb`, `report.md` |
| 5  | Код не свален в один ноутбук: есть внятная структура в `src/`            |        Да        | `src/data_pipeline/`, `src/pipelines/`, `src/retriever/`, `src/service/`, `src/utils/` |
| 6  | Есть Dockerfile **или** понятный сценарий развёртывания без Docker       |        Да        | `Dockerfile` и `docker-compose.yml`, и/или шаги в `README.md`       |
| 7  | Есть `.env.example` и **нет** в репозитории реальных секретов/паролей    |        Да        | `.env.example`, отсутствие `.env`    |
| 8  | Реализованы логи/наблюдаемость (хотя бы консольные логи + `/health`)     |        Да        | Использование `logging`, запись в `artifacts/api.log`, использование `phoenix`, endpoint `/health` |
| 9  | В `report.md` **обоснован выбор финальной модели** по результатам экспериментов |     Да    | `report.md`, разделы про результаты и выбор модели  |
| 10 | `project/README.md` и `report.md` позволяют понять сценарий демонстрации |        Да        | Разделы «Как запустить проект», «Демонстрация на защите»   |

---
